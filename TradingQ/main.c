#include <stdio.h>
#include <stdlib.h>
#include "stocks.h"
#include "stocks.c"
#include "Tradeheader.h"

void test(){
    Queue q = queue_new(5);
    QueueRes p;
    q = queue_a
    printf("Created Successfully");
}
int main()
{

    queue_full();
    queue_empty();
    queue_add(10);
    queue_delete(2);
    queue_add_share(2,4,6);
    queue_length();
    topup(2,4,6);
    return 0;
}
